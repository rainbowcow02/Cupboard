import { Text, View } from 'react-native';

// Unused on web — explore.web.tsx is the active route. Exists so explore.tsx can re-export safely.
export default function ExploreScreen() {
  return (
    <View>
      <Text>Explore</Text>
    </View>
  );
}
